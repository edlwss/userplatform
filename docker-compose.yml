version: "3.9"
services:
  postgres:
    image: postgres:16.13-alpine
    environment:
      POSTGRES_DB: "userplatform"
      POSTGRES_USER: "admin"
      POSTGRES_PASSWORD: "admin"
    ports:
      - "5432:5432"